# scripts/AGENTS.md

Scope: `scripts/**`

- Scripts must be idempotent where practical.
- Avoid destructive defaults.
- Never hardcode secrets or credentials.
- Require explicit repo target arguments for mutating operations.
