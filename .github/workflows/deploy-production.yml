name: Deploy Production

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v6
      - name: Deploy to production
        run: echo "Implement production deploy command"
      - name: Smoke checks
        run: echo "Implement production smoke tests"
